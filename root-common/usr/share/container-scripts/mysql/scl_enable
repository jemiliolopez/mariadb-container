# This will make scl collection binaries work out of box.
unset BASH_ENV PROMPT_COMMAND ENV
# export BASH_ENV=/etc/environment
export ENV=/etc/environment

source scl_source enable ${ENABLED_COLLECTIONS}
